# 부동산 대시보드 UI 단순화 (260708)

> 📁 로컬 경로: /home/choiyh0910/STCode/WSL/90_개인/real_estate_analysis

## 배경

부동산 가격분석 Streamlit 대시보드(app.py, 약 5,000줄)가 기능은 좋으나 "너무 복잡해 보인다"는 문제 제기. 사용자가 지목한 복잡 지점은 ① 상단 KPI 바(시장온도계·평균가격 등 5카드 상시 표시), ② 수요-공급 분석기(변수 80여 종 + 수식 빌더 4개 + 각종 옵션 과부하). 페이지 구조(7페이지)는 이번에 유지하기로 결정.

## 변경 내용

### 1. 전역 KPI 바: 5카드 → 3카드
- 유지: 🌡️ 시장온도계 / 💰 평균가격 / 📊 거래량
- 제거: 전세가율, KB매수우위 카드 (Overview 본문·거래현황 갭분석에 동일 정보 존재)

### 2. 수요-공급 분석기: 프리셋 중심 + 고급 모드 토글
- 기본 화면: **프리셋 지표 선택 1개**(PIR·전세가율·소득대비가격·공급강도) + 차트 설정 + 지표 차트만 표시
- "🔧 고급 모드" 토글 ON 시: 기존 수식 빌더(수식 1~4, 변수 목록 메타테이블, 수식간 듀얼Y축 비교) 전부 재등장 — 기능 손실 없음
- 작성 중이던 수식은 세션에 보존 (토글 껐다 켜도 유지)

### 3. 전세-매매 선행 신호: 기본 접힘 expander로 전환
- 자유차트 페이지에 유사 기능이 있어 시각적 부담만 축소

## 검증

- `py_compile`/`ast.parse` 통과, Streamlit 예외 0건
- Playwright 헤드리스 브라우저로 실화면 검증: KPI 3카드, 기본 모드 화면 구성, 고급 모드 토글 동작, 프리셋 차트 렌더 모두 확인

## 배포

- GitHub `daryh0910/real-estate-analysis` main 브랜치 push 완료 → Streamlit Cloud 자동 재배포
- 원격에 타 PC발 auto 커밋과 히스토리가 갈라져 있었으나, 내용 차이는 이번 작업분뿐임을 확인 후 로컬 유지 병합(`-s ours`)으로 통합

## 구현 방식 메모

- 계획 수립(Fable) → 구현은 Sonnet 서브에이전트에 위임 → 헤드리스 브라우저 검증(Fable) 흐름으로 진행
- 실행 환경: `~/venv`에 requirements.txt 전체 + playwright(chromium) 신규 설치
