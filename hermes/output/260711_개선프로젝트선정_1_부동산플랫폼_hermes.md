# 개선 프로젝트 1: real_estate_analysis
> 선정일: 2026-07-11
> 경로: `/home/younghwan/WSL/90_개인/real_estate_analysis`

## 선정 판정

- **전략적 가치:** 10/10 — 사용자의 핵심 관심사인 TradingView형 부동산 데이터 분석 플랫폼.
- **개선 여지:** 9/10 — 데이터 17~19종과 분석 기능은 풍부하지만 데이터 계약·freshness·point-in-time·UI 회귀검증을 한 번에 통제하는 gate가 부족하다.
- **검증 가능성:** 9/10 — Python·pytest·정적 분석·cache metadata를 WSL에서 재현할 수 있다.
- **Codex Sol 시연 가치:** 9/10 — 복잡한 단일 `app.py`, 데이터 파이프라인, 통계 모듈, 73개 테스트를 함께 다룰 수 있다.

## 이번에 확인한 사실

1. 원본 worktree에 기존 `app.py` 수정이 있어 원본을 건드리지 않았다.
2. baseline pytest는 `72 passed, 1 failed, 4 warnings`다.
3. 실패한 기존 테스트는 첫 번째 `st.tabs()`를 top-level navigation으로 간주한다.
4. 현재 앱은 top-level `st.tabs()`가 아니라 `_PAGES`와 `st.segmented_control()`을 사용한다.
5. 따라서 실패는 현재 UI 설계를 반영하지 못한 **stale regression test**다.
6. cache/data/geo_data에서 분석 대상 파일 48개, 27,266,480 bytes를 확인했다.

## 구현한 개선

- AST 기반으로 `segmented_control`과 `st.tabs`를 구분하는 구조 검사기.
- `_PAGES` 같은 상수 참조를 해석해 실제 primary navigation을 검증.
- nested tab을 top-level tab으로 오인하지 않도록 분리.
- 데이터 payload를 전부 읽지 않고 경로·형식·크기·수정시각을 JSON/Markdown으로 기록.
- offline fixture 6개로 회귀검증.

## 실행 결과

```plain text
신규 Hermes 테스트: 6 passed
실제 앱 navigation: PASS
mode: segmented_control
pages: Overview → 수요공급분석 → 거래현황 → 매물현황 → 매수판단 → 적정가·구매력 → 자유차트
```

## 다음 개선 우선순위

1. **P0 — 원본 stale test 교체**
   - 정규식 기반 `st.tabs()` 검사 대신 Hermes AST 검사 로직을 원본 테스트에 반영.
2. **P0 — 데이터 freshness registry**
   - dataset별 source, observation_date, retrieved_at, expected cadence, stale threshold를 단일 manifest로 관리.
3. **P1 — point-in-time dataset 계약**
   - 분석 기준일 이후 발표된 데이터가 과거 판단에 섞이지 않도록 `available_at`을 보존.
4. **P1 — cache build validation**
   - schema, null ratio, 중복 key, 기간 coverage, 단위, 지역코드 mapping을 build 단계에서 차단.
5. **P2 — app.py 화면 모듈화**
   - 7개 페이지를 독립 renderer로 분리하고 public view model을 테스트.

## 반증·중단 조건

- 단기 목표가 내부 개인 분석에만 있고 외부 배포·반복 사용 계획이 없다면 대규모 모듈화 ROI는 낮다.
- 데이터 source별 관측일·공개일을 확보할 수 없다면 정확한 historical backtest를 주장하면 안 된다.

## 산출물

- `hermes/scripts/260711_real_estate_quality_gate_hermes.py`
- `hermes/tests/260711_test_real_estate_quality_gate_hermes.py`
- `hermes/output/260711_real_estate_quality_gate_hermes.json`
- `hermes/output/260711_real_estate_quality_gate_hermes.md`
